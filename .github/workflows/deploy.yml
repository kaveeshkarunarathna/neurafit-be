name: Deploy

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    # Only deploy after CI passes
    needs: []

    steps:
      - uses: actions/checkout@v4

      # ── Option A: Deploy to Render (recommended) ──
      # Render auto-deploys from GitHub by default.
      # If you prefer manual trigger via API, uncomment below:
      #
      # - name: Deploy to Render
      #   env:
      #     RENDER_DEPLOY_HOOK_URL: ${{ secrets.RENDER_DEPLOY_HOOK_URL }}
      #   run: curl -X POST "$RENDER_DEPLOY_HOOK_URL"

      # ── Option B: Deploy to a VPS via SSH + Docker ──
      # Uncomment and configure secrets: VPS_HOST, VPS_USER, VPS_SSH_KEY
      #
      # - name: Deploy to VPS
      #   uses: appleboy/ssh-action@v1
      #   with:
      #     host: ${{ secrets.VPS_HOST }}
      #     username: ${{ secrets.VPS_USER }}
      #     key: ${{ secrets.VPS_SSH_KEY }}
      #     script: |
      #       cd ~/neurafit-be
      #       git pull origin main
      #       docker compose -f docker-compose.prod.yml up -d --build

      - name: Deployment placeholder
        run: echo "Configure deployment target above (Render or VPS)"
